# Enter your code here. Read input from STDIN. Print output to STDOUT
A = set(map(int,input().split()))
flag = True
for i in range(int(input())):
    flag = flag and A.issuperset(set(map(int,input().split())))

print (flag)
